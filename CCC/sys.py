class buttons:
    def__init__ (self, button_name):

    if st.button(f"button number is {button_name}"):
        st.toast(button_name*button name)

        for i in range(10):
            buttons (i)
